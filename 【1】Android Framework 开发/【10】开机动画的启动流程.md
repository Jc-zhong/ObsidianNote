## 1. 模块代码路径

**bootanimation**    `frameworks/base/cmds/bootanimation`
**surfaceflinger**      `frameworks/native/services/surfaceflinger/`
**init**                        `system/core/init/`

**bootanimation**  编译后会在 `out/target/product/generic_x86/system/bin` 目录下

![[10-bootanimation-out.png]]

## 2. 启动流程详细分析

内核起来后会启动第一个进程，即 **init** 进程。 
**init** 进程会根据 **init.rc** 配置启动 **surfaceflinger** 进程。
```shell
service surfaceflinger /system/bin/surfaceflinger
    class main
    user system
    group graphics drmrpc
    onrestart restart zygote
```

**surfaceflinger**  进程便启动了，跟着就会跑进程的 **main()** 函数
`frameworks/native/services/surfaceflinger/main_surfaceflinger.cpp`

```c++
int main(){
....
	// instantiate surfaceflinger
	// 创建 surfaceflinger 服务实例
	sp<SurfaceFlinger> flinger = new SurfaceFlinger();	
....
	flinger->init();
	
	// publish surface filnger
	sp<IServiceManager> sm(defaultServiceManager());
	// 将 surfaceflinger 注册到 service manager 里面
	sm->addService(String16(SurfaceFlinger::getServiceName()),flinger,false);
	
	// run in this thread
	flinger->run();
	
	return 0;
}
```

`frameworks/native/services/surfaceflinger/Surfaceflinger.cpp`

```c++
	// Do not call property_set on main thread which be blocked by init
	// Use StartPropertySetThread instead.
	void SurfaceFlinger::init(){
		ALOGI("SurfaceFlinger`s main thread ready to run.");
		ALOGI("Initializing graphics H/W...");
		
		// Inform native graphics APIs whether the present timestamp is supported"
		if(getHwComposer().hasCapability(
			HWC2::Capability::PresentFenceIsNotReliable)){
			mStartPropertySetThread = new StartPropertySetThread(false);
		} else {
			mStartPropertySetThread = new StartPropertySetThread(true);
		}
		
		// 真正启动设置 bootanimation 的属性线程
		if(mStartPropertySetThread->Start() != NO_ERROR){
			ALOGE("Run StartPropertySetThread failed!");
		}
		
		ALOGV("Done initializing");
	}
```

初始化 graphics 后， mStartPropertySetThread() 播放开机动画。注意：不是以前的 startBootAnim 方法

StartPropertySetThread 如下定：

```c++
StartPropertySetThread::StartPropertySetThread(bool timestampPropertyValue):
        Thread(false), mTimestampPropertyValue(timestampPropertyValue) {}

status_t StartPropertySetThread::Start() {
    return run("SurfaceFlinger::StartPropertySetThread", PRIORITY_NORMAL);
}

bool StartPropertySetThread::threadLoop() {
    // Set property service.sf.present_timestamp, consumer need check its readiness
    property_set(kTimestampProperty, mTimestampPropertyValue ? "1" : "0");
    // Clear BootAnimation exit flag
    property_set("service.bootanim.exit", "0");     // 关键属性
    property_set("service.bootanim.progress", "0");
    // Start BootAnimation if not started
    property_set("ctl.start", "bootanim");  // 关键属性
    // Exit immediately
    return false;
}
```

这样 **bootanimation** 进程就会启动。
（思考：为什么设置了一个属性后就能启动 **bootanimation** ？）

接下来看 `/system/core/init/init.cpp` 的 main 函数中

```c++
int main(int argc, char** argv){
....
	property_load_boot_defaults();
	export_oem_lock_status();
	start_property_service();   // 关键代码
	set_usb_controller();
....
}
```

下面来看看 `start_property_service()`  方法，
在 `/system/core/init/property_service.cpp` 目录下

```c++
void start_property_service(){
	property_set("ro.property_service.version","2");
	property_set_fd = create_socket(PROP_SERVICE_NAME, SOCK_STEAM | SOCK_CLOEXEC | 
			SOCK_NONBLOCK, 0666, 0, 0. NULL);
	if(property_set_fd == -1){
		PLOG(ERROR) << "start_property_sercie socket creation failed";
		exit(1);
	}
	
	listen(property_set_fd, 8);
	register_epoll_handler(property_set_fd, handle_property_set_fd);
}
```

**init** 进程会使用 **epoll** 机制来轮询事件，其中一个事件是系统属性值被修改。
得知该事件后，会执行 **handle_property_set_fd()**  ，而在此函数内会执行  `property_service.cpp` 的 **handle_property_set()** 函数，再由此函数去调用 `init.cpp` 中的**handle_control_message()** 函数，进而启动 **bootanimation** 进程
（此处就省略各个函数的调用截图，自行到源码中&结合 `grep | xxx ./ -rn` 指令跟踪）

总结流程图：

![[10-bootanimation-processon.png]]

